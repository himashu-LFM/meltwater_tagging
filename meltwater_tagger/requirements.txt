anthropic>=0.92.0
# Pinned to match the Docker base image tag (mcr.microsoft.com/playwright/python
# :v1.61.0-jammy) so the bundled Chromium + system libs always match the client.
# If you bump this, bump the Dockerfile FROM tag to the same version.
playwright==1.61.0
pandas>=2.0
openpyxl>=3.1
httpx>=0.27
flask>=3.0
gunicorn>=21.2 ; sys_platform != "win32"
waitress>=3.0
supabase>=2.7
